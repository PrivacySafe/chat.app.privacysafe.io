import { State } from './types'

export const state: State = {
  connectivityStatus: 'offline',
  user: '',
  lang: 'en',
  theme: 'default',
  colors: {},
  appWindowSize: {
    width: 0,
    height: 0,
  }
}
