import type { State } from './types';

export const state: State = {
  appVersion: '',
  connectivityStatus: 'offline',
  user: '',
  lang: 'en',
  colorTheme: 'default',
  appWindowSize: {
    width: 0,
    height: 0,
  },
};
